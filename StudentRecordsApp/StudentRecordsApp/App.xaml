<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StudentRecordsApp.App">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Page Margin -->
            <Thickness x:Key="PageMargin">20</Thickness>
            <!-- Colors -->
            <Color x:Key="AppPrimaryColor"> #594043</Color>
            <Color x:Key="AppBackgroundColor">#f2e0cf</Color>
            <Color x:Key="PrimaryColor">Black</Color>
            <Color x:Key="ButtonColor">#ed6830</Color>
            <Color x:Key="SecondaryColor">#b3a89b</Color>
            <Color x:Key="TertiaryColor">Silver</Color>

            <Style TargetType="NavigationPage">
                <Setter Property="BarBackgroundColor" Value="{StaticResource AppPrimaryColor}"/>
                <Setter Property="BarTextColor" Value="White" />
            </Style>
            
            <Style TargetType="Button">
                <Setter Property="FontSize"
                        Value="Medium" />
                <Setter Property="BackgroundColor"
                        Value="{StaticResource ButtonColor}" />
                <Setter Property="TextColor"
                        Value="White" />
                <Setter Property="CornerRadius"
                        Value="5" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>